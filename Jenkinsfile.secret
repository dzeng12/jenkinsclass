pipeline {
    agent any
    stages {
        stage('Print Secret') {
            steps {
                withCredentials([string(credentialsId: 'secret_ingredient', variable: 'SECRET')]) {
                    sh 'echo "The secret in our hot chocolate recipe is: ${SECRET}"'
                }
            }
        }
    }
}